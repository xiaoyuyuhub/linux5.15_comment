\mbox{\hyperlink{gup_8c_aa4595a7163e71ca3648e5b04b50fc732}{try\+\_\+grab\+\_\+compound\+\_\+head()}} -\/ attempt to elevate a page\textquotesingle{}s refcount, by a flags-\/dependent amount.

Even though the name includes \char`\"{}compound\+\_\+head\char`\"{}, this function is still appropriate for callers that have a non-\/compound

pages according to @gfp\+\_\+mask if necessary. if @mm is N\+U\+LL, try to charge to the active memcg.

Do not use this for pages allocated for swapin.

Returns 0 on success. Otherwise, an error code is returned.

This is done to accelerate the reclaim of

Page may still be unevictable for other reasons.

lru\+\_\+lock must not be held, interrupts must be enabled. 