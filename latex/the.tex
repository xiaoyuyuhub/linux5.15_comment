page\+\_\+is\+\_\+file\+\_\+lru -\/ should the page be on a file L\+RU or anon L\+RU?

Returns 1 if

\+\_\+\+\_\+clear\+\_\+page\+\_\+lru\+\_\+flags -\/ clear page lru flags before releasing a page

page\+\_\+lru -\/ which L\+RU list should a page be on?

Returns the L\+RU list a page should be on, as an index into the array of L\+RU lists.

struct \mbox{\hyperlink{structnand__pos}{nand\+\_\+pos}} -\/ N\+A\+ND position object @target\+: the N\+A\+ND target/die @lun\+: the L\+UN identifier @plane\+: the plane within the L\+UN @eraseblock\+: the eraseblock within the L\+UN

These information are usually used by specific sub-\/layers to select the appropriate target/die and generate a row address to pass to the device.

page\+\_\+mkwrite\+\_\+check\+\_\+truncate -\/ check if page was truncated

@inode\+: the inode to check the page against

Returns the number of bytes in the page up to E\+OF, or -\/E\+F\+A\+U\+LT if the page was truncated.

struct \mbox{\hyperlink{structpipe__buffer}{pipe\+\_\+buffer}} -\/ a linux kernel pipe buffer

@offset\+: offset of data inside the

delete\+\_\+from\+\_\+page\+\_\+cache -\/ delete page from page cache

This must be called only on pages that have been verified to be in the page cache and locked. It will never put the page into the free list, the caller has a reference on the page.

unlock\+\_\+page -\/ unlock a locked page

Unlocks the page and wakes up sleepers in \mbox{\hyperlink{linux_2pagemap_8h_a405f54ac2a8acbc7efe30bd63d042bf0}{wait\+\_\+on\+\_\+page\+\_\+locked()}}. Also wakes sleepers in \mbox{\hyperlink{linux_2pagemap_8h_a29d56381a70b8a066eb3fde1b03fa797}{wait\+\_\+on\+\_\+page\+\_\+writeback()}} because the wakeup mechanism between Page\+Locked pages and Page\+Writeback pages is shared. But that\textquotesingle{}s OK -\/ sleepers in \mbox{\hyperlink{linux_2pagemap_8h_a29d56381a70b8a066eb3fde1b03fa797}{wait\+\_\+on\+\_\+page\+\_\+writeback()}} just go back to sleep.

Note that this depends on P\+G\+\_\+waiters being the sign bit in the byte that \mbox{\hyperlink{protocolcontains-p}{contains}} P\+G\+\_\+locked -\/ thus the \mbox{\hyperlink{build__bug_8h_a0fc08f11f6c83dbc94a75ca49d29fae8}{B\+U\+I\+L\+D\+\_\+\+B\+U\+G\+\_\+\+O\+N()}}. That allows us to clear the P\+G\+\_\+locked bit and test P\+G\+\_\+waiters at the same time fairly portably (architectures that do L\+L/\+SC can test any bit, while x86 can test the sign bit).

end\+\_\+page\+\_\+writeback -\/ end writeback against a page

\mbox{\hyperlink{filemap_8c_a36341f6bc88dec984fc75fc7a033246e}{try\+\_\+to\+\_\+release\+\_\+page()}} -\/ release old fs-\/specific metadata on a page

@gfp\+\_\+mask\+: memory allocation flags (and I/O mode)

\mbox{\hyperlink{classThe}{The}} \mbox{\hyperlink{structaddress__space}{address\+\_\+space}} is to try to release any data against the page (presumably at page-\/$>$private).

This may also be called if P\+G\+\_\+fscache is set on a page, indicating that the page is known to the local caching routines.

\mbox{\hyperlink{classThe}{The}} @gfp\+\_\+mask argument specifies whether I/O may be performed to release this page (\+\_\+\+\_\+\+G\+F\+P\+\_\+\+IO), and whether the call may block (\+\_\+\+\_\+\+G\+F\+P\+\_\+\+R\+E\+C\+L\+A\+IM \& \+\_\+\+\_\+\+G\+F\+P\+\_\+\+FS).

Return\+: \%1 if the release was successful, otherwise return zero.

\mbox{\hyperlink{gup_8c_aaf10d99f0bc8b4e5f7fde4d3da2a84b6}{unpin\+\_\+user\+\_\+page\+\_\+range\+\_\+dirty\+\_\+lock()}} -\/ release and optionally dirty gup-\/pinned page range

@npages\+: number of consecutive pages to release. @make\+\_\+dirty\+: whether to mark the pages dirty

\char`\"{}gup-\/pinned page range\char`\"{} refers to a range of pages that has had one of the \mbox{\hyperlink{gup_8c_a42ce93a9eb4e23e2806c1f244fc61ee4}{pin\+\_\+user\+\_\+pages()}} variants called on that page.

For the page ranges defined by \mbox{[}page .. page+npages\mbox{]}, make that range (or its head pages, if a compound page) dirty, if @make\+\_\+dirty is true, and if the page range was previously listed as clean.

\mbox{\hyperlink{page-writeback_8c_add65097ae40e9c74fc839379ea5f23a1}{set\+\_\+page\+\_\+dirty\+\_\+lock()}} is used internally. If instead, \mbox{\hyperlink{page-writeback_8c_a7336f3a590bed267e17c0c13752753db}{set\+\_\+page\+\_\+dirty()}} is required, then the caller should a) verify that this is really correct, because \+\_\+lock() is usually required, and b) hand code it\+: \mbox{\hyperlink{page-writeback_8c_add65097ae40e9c74fc839379ea5f23a1}{set\+\_\+page\+\_\+dirty\+\_\+lock()}}, \mbox{\hyperlink{gup_8c_a660b424e7f87f459ffb8828df2a1bc75}{unpin\+\_\+user\+\_\+page()}}.

page\+\_\+evictable -\/ test whether a page is evictable

Test whether page is evictable--i.\+e., should be placed on active/inactive lists vs unevictable list.

Reasons page might not be evictable\+: (1) page\textquotesingle{}s mapping marked unevictable (2) page is part of an mlocked V\+MA

replace\+\_\+page -\/ replace page in vma by new ksm page @vma\+: vma that holds the pte pointing to page

@kpage\+: the ksm page we replace page by @orig\+\_\+pte\+: the original value of the pte

Returns 0 on success, -\/E\+F\+A\+U\+LT on failure.

page\+\_\+cgroup\+\_\+ino -\/ return inode number of the memcg a page is charged to

Look up the closest online ancestor of the memory cgroup

lock\+\_\+page\+\_\+lruvec -\/ lock and return lruvec for a given page.

These functions are safe to use under any of the following conditions\+:
\begin{DoxyItemize}
\item page locked
\item Page\+L\+RU cleared
\item \mbox{\hyperlink{memcontrol_8c_a0c77f273151d224eb55f369aa825d348}{lock\+\_\+page\+\_\+memcg()}}
\item page-\/$>$\+\_\+refcount is zero
\end{DoxyItemize}

lock\+\_\+page\+\_\+memcg -\/ lock a page and memcg binding

This function protects unlocked L\+RU pages from being moved to another cgroup.

It ensures lifetime of the locked memcg. Caller is responsible for the lifetime of the page.

unlock\+\_\+page\+\_\+memcg -\/ unlock a page and memcg binding

write\+\_\+one\+\_\+page -\/ write out a single page and wait on I/O

\mbox{\hyperlink{classThe}{The}} page must be locked by the caller and will be unlocked upon return.

Note that the mapping\textquotesingle{}s A\+S\+\_\+\+E\+I\+O/\+A\+S\+\_\+\+E\+N\+O\+S\+PC flags will be cleared when this function returns.

Return\+: \%0 on success, negative error code otherwise

page\+\_\+mapped\+\_\+in\+\_\+vma -\/ check whether a page is really mapped in a V\+MA

@vma\+: the V\+MA to test

Returns 1 if the page is mapped into the page tables of the V\+MA, 0 if the page is not mapped into the page tables of this V\+MA. Only valid for normal file or anonymous V\+M\+As.

page\+\_\+referenced -\/ test if the page was referenced

@is\+\_\+locked\+: caller holds lock on the page @memcg\+: target memory cgroup @vm\+\_\+flags\+: collect encountered vma-\/$>$vm\+\_\+flags who actually referenced the page

Quick test\+\_\+and\+\_\+clear\+\_\+referenced for all mappings to a page, returns the number of ptes which referenced the page.

page\+\_\+move\+\_\+anon\+\_\+rmap -\/ move a page to our \mbox{\hyperlink{structanon__vma}{anon\+\_\+vma}}

@vma\+: the vma the page belongs to

When a page belongs exclusively to one process after a C\+OW event, that page can be moved into the \mbox{\hyperlink{structanon__vma}{anon\+\_\+vma}} that belongs to just that process, so the rmap code will not search the parent or sibling processes.

\+\_\+\+\_\+page\+\_\+check\+\_\+anon\+\_\+rmap -\/ sanity check anonymous rmap addition

@vma\+: the vm area in which the mapping is added @address\+: the user virtual address mapped

page\+\_\+add\+\_\+anon\+\_\+rmap -\/ add pte mapping to an anonymous page

@vma\+: the vm area in which the mapping is added @address\+: the user virtual address mapped @compound\+: charge the page as compound or small page

\mbox{\hyperlink{classThe}{The}} caller needs to hold the pte lock, and the page must be locked in the \mbox{\hyperlink{structanon__vma}{anon\+\_\+vma}} case\+: to serialize mapping,index checking after setting, and to ensure that Page\+Anon is not being upgraded racily to Page\+Ksm (but Page\+Ksm is never downgraded to Page\+Anon).

page\+\_\+add\+\_\+new\+\_\+anon\+\_\+rmap -\/ add pte mapping to a new anonymous page

@vma\+: the vm area in which the mapping is added @address\+: the user virtual address mapped @compound\+: charge the page as compound or small page

Same as page\+\_\+add\+\_\+anon\+\_\+rmap but must only be called on {\itshape new} pages. This means the inc-\/and-\/test can be bypassed. Page does not have to be locked.

page\+\_\+add\+\_\+file\+\_\+rmap -\/ add pte mapping to a file page

@compound\+: charge the page as compound or small page

\mbox{\hyperlink{classThe}{The}} caller needs to hold the pte lock.

try\+\_\+to\+\_\+unmap -\/ try to remove all page table mappings to a page

@flags\+: action and flags

Tries to remove all the page table entries which are mapping this page, used in the pageout path. Caller must hold the page lock.

It is the caller\textquotesingle{}s responsibility to check if the page is still mapped when needed (use T\+T\+U\+\_\+\+S\+Y\+NC to prevent accounting races).

try\+\_\+to\+\_\+migrate -\/ try to replace all page table mappings with swap entries

@flags\+: action and flags

Tries to remove all the page table entries which are mapping this page and replace them with special swap entries. Caller must hold the page lock.

page\+\_\+mlock -\/ try to mlock a page

Called from munlock code. Checks all of the V\+M\+As mapping the page and mlocks the page if any are found. \mbox{\hyperlink{classThe}{The}} page will be returned with P\+G\+\_\+mlocked cleared if it is not mapped by any locked vmas.

lru\+\_\+cache\+\_\+add -\/ add a page to a page list

Queue the page for addition to the L\+RU via pagevec. \mbox{\hyperlink{classThe}{The}} decision on whether to add the page to the \mbox{[}in\mbox{]}active \mbox{[}file$\vert$anon\mbox{]} list is deferred until the pagevec is drained. This gives a chance for the caller of \mbox{\hyperlink{mm_2swap_8c_a24a0c922978df84d2f84fd32fd90da3c}{lru\+\_\+cache\+\_\+add()}} have the page added to the active list using \mbox{\hyperlink{mm_2swap_8c_ac0483ff2c88088a8db13e730c30645af}{mark\+\_\+page\+\_\+accessed()}}.

lru\+\_\+cache\+\_\+add\+\_\+inactive\+\_\+or\+\_\+unevictable

@vma\+: vma in which page is mapped for determining reclaimability

Place

do\+\_\+invalidatepage -\/ invalidate part or all of a page

@offset\+: start of the range to invalidate @length\+: length of the range to invalidate

\mbox{\hyperlink{truncate_8c_ac24d0b0d7749ee82f94a553cde76d7f3}{do\+\_\+invalidatepage()}} is called when all or part of the page has become invalidated by a truncate operation.

\mbox{\hyperlink{truncate_8c_ac24d0b0d7749ee82f94a553cde76d7f3}{do\+\_\+invalidatepage()}} does not have to release all buffers, but it must ensure that no dirty buffer is left outside @offset and that no I/O is underway against any of the blocks which are outside the truncation point. Because the caller is about to free (and possibly reuse) those blocks on-\/disk.

workingset\+\_\+eviction -\/ note the eviction of a page from memory @target\+\_\+memcg\+: the cgroup that is causing the reclaim

Return\+: a shadow entry to be stored in

workingset\+\_\+refault -\/ evaluate the refault of a previously evicted page

@shadow\+: shadow entry of the evicted page

Calculates and evaluates the refault distance of the previously evicted page in the context of the node and the memcg whose memory pressure caused the eviction. 