struct \mbox{\hyperlink{structttm__bo__kmap__obj}{ttm\+\_\+bo\+\_\+kmap\+\_\+obj}}

@virtual\+: \mbox{\hyperlink{classThe}{The}} current kernel virtual address.

@bo\+\_\+kmap\+\_\+type\+: \mbox{\hyperlink{protocolType-p}{Type}} of bo\+\_\+kmap.

Object describing a kernel mapping. Since a T\+TM bo may be located in various memory types with various caching policies, the mapping can either be an ioremap, a vmap, a kmap or part of a premapped region.

fscache\+\_\+end\+\_\+io -\/ End a retrieval operation on a page @op\+: \mbox{\hyperlink{classThe}{The}} F\+S-\/\+Cache operation covering the retrieval

@error\+: \mbox{\hyperlink{classThe}{The}} error code (0 if successful)

Note the end of an operation to retrieve a page, as covered by a particular operation record.

fscache\+\_\+read\+\_\+or\+\_\+alloc\+\_\+page -\/ Read a page from the cache or allocate a block in which to store it @cookie\+: \mbox{\hyperlink{classThe}{The}} cookie representing the cache object

@end\+\_\+io\+\_\+func\+: \mbox{\hyperlink{classThe}{The}} callback to invoke when and if the page is filled @context\+: An arbitrary piece of data to pass on to end\+\_\+io\+\_\+func() @gfp\+: \mbox{\hyperlink{classThe}{The}} conditions under which memory allocation should be made

Read a page from the cache, or if that\textquotesingle{}s not possible make a potential one-\/block reservation in the cache into which the page may be stored once fetched from the server.

If the page is not backed by the cache object, or if it there\textquotesingle{}s some reason it can\textquotesingle{}t be, -\/E\+N\+O\+B\+U\+FS will be returned and nothing more will be done for that page.

Else, if that page is backed by the cache, a read will be initiated directly to the netfs\textquotesingle{}s page and 0 will be returned by this function. \mbox{\hyperlink{classThe}{The}} end\+\_\+io\+\_\+func() callback will be invoked when the operation terminates on a completion or failure. Note that the callback may be invoked before the return.

Else, if the page is unbacked, -\/E\+N\+O\+D\+A\+TA is returned and a block may have been allocated in the cache.

See Documentation/filesystems/caching/netfs-\/api.\+rst for a complete description.

fscache\+\_\+alloc\+\_\+page -\/ Allocate a block in which to store a page @cookie\+: \mbox{\hyperlink{classThe}{The}} cookie representing the cache object

@gfp\+: \mbox{\hyperlink{classThe}{The}} conditions under which memory allocation should be made

Request Allocation a block in the cache in which to store a netfs page without retrieving any contents from the cache.

If the page is not backed by a file then -\/E\+N\+O\+B\+U\+FS will be returned and nothing more will be done, and no reservation will be made.

Else, a block will be allocated if one wasn\textquotesingle{}t already, and 0 will be returned

See Documentation/filesystems/caching/netfs-\/api.\+rst for a complete description.

fscache\+\_\+write\+\_\+page -\/ Request storage of a page in the cache @cookie\+: \mbox{\hyperlink{classThe}{The}} cookie representing the cache object

@object\+\_\+size\+: Updated size of object @gfp\+: \mbox{\hyperlink{classThe}{The}} conditions under which memory allocation should be made

Request the contents of the netfs page be written into the cache. This request may be ignored if no cache block is currently allocated, in which case it will return -\/E\+N\+O\+B\+U\+FS.

If a cache block was already allocated, a write will be initiated and 0 will be returned. \mbox{\hyperlink{classThe}{The}} P\+G\+\_\+fscache\+\_\+write page bit is set immediately and will then be cleared at the completion of the write to indicate the success or failure of the operation. Note that the completion may happen before the return.

See Documentation/filesystems/caching/netfs-\/api.\+rst for a complete description.

fscache\+\_\+uncache\+\_\+page -\/ Indicate that caching is no longer required on a page @cookie\+: \mbox{\hyperlink{classThe}{The}} cookie representing the cache object

Tell the cache that we no longer want a page to be cached and that it should remove any knowledge of the netfs page it may have.

Note that this cannot cancel any outstanding I/O operations between this page and the cache.

See Documentation/filesystems/caching/netfs-\/api.\+rst for a complete description.

fscache\+\_\+check\+\_\+page\+\_\+write -\/ Ask if a page is being writing to the cache @cookie\+: \mbox{\hyperlink{classThe}{The}} cookie representing the cache object

Ask the cache if a page is being written to the cache.

See Documentation/filesystems/caching/netfs-\/api.\+rst for a complete description.

fscache\+\_\+wait\+\_\+on\+\_\+page\+\_\+write -\/ Wait for a page to complete writing to the cache @cookie\+: \mbox{\hyperlink{classThe}{The}} cookie representing the cache object

Ask the cache to wake us up when a page is no longer being written to the cache.

See Documentation/filesystems/caching/netfs-\/api.\+rst for a complete description.

fscache\+\_\+maybe\+\_\+release\+\_\+page -\/ Consider releasing a page, cancelling a store @cookie\+: \mbox{\hyperlink{classThe}{The}} cookie representing the cache object

@gfp\+: \mbox{\hyperlink{classThe}{The}} gfp flags passed to releasepage()

Consider releasing a page for the vmscan algorithm, on behalf of the netfs\textquotesingle{}s releasepage() call. A storage request on the page may cancelled if it is not currently being processed.

\mbox{\hyperlink{classThe}{The}} function returns true if the page no longer has a storage request on it, and false if a storage request is left in place. If true is returned, the page will have been passed to \mbox{\hyperlink{linux_2fscache_8h_a82d111ab184430c7f4e76372576cb69b}{fscache\+\_\+uncache\+\_\+page()}}. If false is returned the page cannot be freed yet.

set\+\_\+page\+\_\+fscache -\/ Set P\+G\+\_\+fscache on a page and take a ref

Set the P\+G\+\_\+fscache (P\+G\+\_\+private\+\_\+2) flag on a page and take the reference needed for the VM to handle its lifetime correctly. This sets the flag and takes the reference unconditionally, so care must be taken not to set the flag again if it\textquotesingle{}s already set.

end\+\_\+page\+\_\+fscache -\/ Clear P\+G\+\_\+fscache and release any waiters

Clear the P\+G\+\_\+fscache (P\+G\+\_\+private\+\_\+2) bit on a page and wake up any sleepers waiting for this. \mbox{\hyperlink{classThe}{The}} page ref held for P\+G\+\_\+private\+\_\+2 being set is released.

This is, for example, used when a netfs page is being written to a local disk cache, thereby allowing writes to the cache for the same page to be serialised.

wait\+\_\+on\+\_\+page\+\_\+fscache -\/ Wait for P\+G\+\_\+fscache to be cleared on a page

Wait for P\+G\+\_\+fscache (aka P\+G\+\_\+private\+\_\+2) to be cleared on a page.

wait\+\_\+on\+\_\+page\+\_\+fscache\+\_\+killable -\/ Wait for P\+G\+\_\+fscache to be cleared on a page

Wait for P\+G\+\_\+fscache (aka P\+G\+\_\+private\+\_\+2) to be cleared on a page or until a fatal signal is received by the calling task.

Return\+:
\begin{DoxyItemize}
\item 0 if successful.
\item -\/E\+I\+N\+TR if a fatal signal was encountered.
\end{DoxyItemize}

page\+\_\+has\+\_\+private -\/ Determine if page has private stuff

Determine if a page has private stuff, indicating that release routines should be invoked upon it.

set\+\_\+page\+\_\+private\+\_\+2 -\/ Set P\+G\+\_\+private\+\_\+2 on a page and take a ref

Set the P\+G\+\_\+private\+\_\+2 flag on a page and take the reference needed for the VM to handle its lifetime correctly. This sets the flag and takes the reference unconditionally, so care must be taken not to set the flag again if it\textquotesingle{}s already set.

@index\+: Index of first page to be read. @req\+\_\+count\+: Total number of pages being read by the caller.

\mbox{\hyperlink{linux_2pagemap_8h_ac03057f36147134faa0fdd8ad9d4f7eb}{page\+\_\+cache\+\_\+async\+\_\+readahead()}} should be called when a page is used which is marked as Page\+Readahead; this is a marker to suggest that the application has used up enough of the readahead window that we should start pulling in more pages.

i\+\_\+blocks\+\_\+per\+\_\+page -\/ How many blocks fit in this page. @inode\+: \mbox{\hyperlink{classThe}{The}} inode which \mbox{\hyperlink{protocolcontains-p}{contains}} the blocks.

If the block size is larger than the size of this page, return zero.

Context\+: \mbox{\hyperlink{classThe}{The}} caller should hold a refcount on the page to prevent it from being split. Return\+: \mbox{\hyperlink{classThe}{The}} number of filesystem blocks covered by this page.

sg\+\_\+assign\+\_\+page -\/ Assign a given page to an SG entry @sg\+: SG entry

Description\+: Assign page to sg entry. Also see \mbox{\hyperlink{scatterlist_8h_a239b3f3fcb0ed4d33b62648ad4679429}{sg\+\_\+set\+\_\+page()}}, the most commonly used variant.

sg\+\_\+set\+\_\+page -\/ Set sg entry to point at given page @sg\+: SG entry

@len\+: Length of data @offset\+: Offset into page

Description\+: Use this function to set an sg entry pointing at a page, never assign the page directly. We encode sg table information in the lower bits of the page pointer. See \mbox{\hyperlink{scatterlist_8h_aad7aad94cc6e462737b2ae514fa9a6eb}{sg\+\_\+page()}} for looking up the page belonging to an sg entry.

ib\+\_\+dma\+\_\+map\+\_\+page -\/ Map a physical page to D\+MA address @dev\+: \mbox{\hyperlink{classThe}{The}} device for which the dma\+\_\+addr is to be created

@offset\+: \mbox{\hyperlink{classThe}{The}} offset within the page @size\+: \mbox{\hyperlink{classThe}{The}} size of the region in bytes @direction\+: \mbox{\hyperlink{classThe}{The}} direction of the D\+MA

struct \mbox{\hyperlink{structfcoe__percpu__s}{fcoe\+\_\+percpu\+\_\+s}} -\/ \mbox{\hyperlink{classThe}{The}} context for F\+CoE receive thread(s) @kthread\+: \mbox{\hyperlink{classThe}{The}} thread context (used by bnx2fc) @work\+: \mbox{\hyperlink{classThe}{The}} work item (used by fcoe) @fcoe\+\_\+rx\+\_\+list\+: \mbox{\hyperlink{classThe}{The}} queue of pending packets to process

@crc\+\_\+eof\+\_\+offset\+: \mbox{\hyperlink{classThe}{The}} offset into the C\+RC page pointing to available memory for a new trailer

put\+\_\+and\+\_\+wait\+\_\+on\+\_\+page\+\_\+locked -\/ Drop a reference and wait for it to be unlocked

@state\+: \mbox{\hyperlink{classThe}{The}} sleep state (T\+A\+S\+K\+\_\+\+K\+I\+L\+L\+A\+B\+LE, T\+A\+S\+K\+\_\+\+U\+N\+I\+N\+T\+E\+R\+R\+U\+P\+T\+I\+B\+LE, etc).

\mbox{\hyperlink{classThe}{The}} caller should hold a reference on

end\+\_\+page\+\_\+private\+\_\+2 -\/ Clear P\+G\+\_\+private\+\_\+2 and release any waiters

Clear the P\+G\+\_\+private\+\_\+2 bit on a page and wake up any sleepers waiting for this. \mbox{\hyperlink{classThe}{The}} page ref held for P\+G\+\_\+private\+\_\+2 being set is released.

This is, for example, used when a netfs page is being written to a local disk cache, thereby allowing writes to the cache for the same page to be serialised.

wait\+\_\+on\+\_\+page\+\_\+private\+\_\+2 -\/ Wait for P\+G\+\_\+private\+\_\+2 to be cleared on a page

Wait for P\+G\+\_\+private\+\_\+2 (aka P\+G\+\_\+fscache) to be cleared on a page.

wait\+\_\+on\+\_\+page\+\_\+private\+\_\+2\+\_\+killable -\/ Wait for P\+G\+\_\+private\+\_\+2 to be cleared on a page

Wait for P\+G\+\_\+private\+\_\+2 (aka P\+G\+\_\+fscache) to be cleared on a page or until a fatal signal is received by the calling task.

Return\+:
\begin{DoxyItemize}
\item 0 if successful.
\item -\/E\+I\+N\+TR if a fatal signal was encountered.
\end{DoxyItemize}

\mbox{\hyperlink{mm_2memory_8c_a4d439e775b6cc8d8d9b4850f51e6e563}{unmap\+\_\+mapping\+\_\+page()}} -\/ Unmap single page from processes.

Unmap this page from any userspace process which still has it mmaped. Typically, for efficiency, the range of nearby pages has already been unmapped by \mbox{\hyperlink{mm_2memory_8c_afb3cdfd244b83569cc07806d0f3420da}{unmap\+\_\+mapping\+\_\+pages()}} or \mbox{\hyperlink{mm_2memory_8c_acdab5131c868494117512298740bcc4b}{unmap\+\_\+mapping\+\_\+range()}}. But once truncation or invalidation holds the lock on a page, it may find that the page has been remapped again\+: and then uses \mbox{\hyperlink{mm_2memory_8c_a4d439e775b6cc8d8d9b4850f51e6e563}{unmap\+\_\+mapping\+\_\+page()}} to unmap it finally.

\mbox{\hyperlink{linux_2pagemap_8h_a6894dab407ad5c4678e36da2e2ea1c5c}{wait\+\_\+for\+\_\+stable\+\_\+page()}} -\/ wait for writeback to finish, if necessary.

This function determines if the given page is related to a backing device that requires page contents to be held stable during writeback. If so, then it will wait for any pending writeback to complete.

get\+\_\+pfnblock\+\_\+flags\+\_\+mask -\/ Return the requested group of flags for the pageblock\+\_\+nr\+\_\+pages block of pages

@pfn\+: \mbox{\hyperlink{classThe}{The}} target page frame number @mask\+: mask of bits that the caller is interested in

Return\+: pageblock\+\_\+bits flags

set\+\_\+pfnblock\+\_\+flags\+\_\+mask -\/ Set the requested group of flags for a pageblock\+\_\+nr\+\_\+pages block of pages

@flags\+: \mbox{\hyperlink{classThe}{The}} flags to set @pfn\+: \mbox{\hyperlink{classThe}{The}} target page frame number @mask\+: mask of bits that the caller is interested in

\+\_\+\+\_\+free\+\_\+pages -\/ Free pages allocated with \mbox{\hyperlink{mempolicy_8c_add7a36e6a0c552051c3453df754b9c7b}{alloc\+\_\+pages()}}.

@order\+: \mbox{\hyperlink{classThe}{The}} order of the allocation.

This function can free multi-\/page allocations that are not compound pages. It does not check that the @order passed in matches that of the allocation, so it is easy to leak memory. Freeing more memory than was allocated will probably emit a warning.

If the last reference to this page is speculative, it will be released by put\+\_\+page() which only frees the first page of a non-\/compound allocation. To prevent the remaining pages from being leaked, we free the subsequent pages here. If you want to use the page\textquotesingle{}s reference count to decide when to free the allocation, you should allocate a compound page, and use put\+\_\+page() instead of \mbox{\hyperlink{page__alloc_8c_a09a84df4c6cb817075a2eece7c7c0345}{\+\_\+\+\_\+free\+\_\+pages()}}.

Context\+: May be called in interrupt context or while holding a normal spinlock, but not in N\+MI context or while holding a raw spinlock. 