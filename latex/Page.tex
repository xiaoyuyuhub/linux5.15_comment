struct \mbox{\hyperlink{structethtool__module__eeprom}{ethtool\+\_\+module\+\_\+eeprom}} -\/ E\+E\+P\+R\+OM dump from specified page @offset\+: Offset within the specified E\+E\+P\+R\+OM page to begin read, in bytes. @length\+: Number of bytes to read.

@bank\+: Page bank number to read from, if applicable by E\+E\+P\+R\+OM spec. @i2c\+\_\+address\+: I2C address of a page. Value less than 0x7f expected. Most E\+E\+P\+R\+O\+Ms use 0x50 or 0x51. @data\+: Pointer to buffer with E\+E\+P\+R\+OM data of @length size.

This can be used to manage pages during E\+E\+P\+R\+OM dump in ethtool and pass required information to the driver.

attach\+\_\+page\+\_\+private -\/ Attach private data to a page.

@data\+: Data to attach to page.

Attaching private data to a page increments the page\textquotesingle{}s reference count. \mbox{\hyperlink{classThe}{The}} data must be detached before the page will be freed.

detach\+\_\+page\+\_\+private -\/ Detach private data from a page.

Removes the data that was previously attached to the page and decrements the refcount on the page.

Return\+: Data that was attached to the page.

add\+\_\+page\+\_\+wait\+\_\+queue -\/ Add an arbitrary waiter to a page\textquotesingle{}s wait queue

@waiter\+: Waiter to add to the queue

Add an arbitrary @waiter to the wait queue for the nominated

\+\_\+\+\_\+putback\+\_\+isolated\+\_\+page -\/ Return a now-\/isolated page back where we got it

@order\+: Order of the isolated page @mt\+: \mbox{\hyperlink{classThe}{The}} page\textquotesingle{}s pageblock\textquotesingle{}s migratetype

This function is meant to return a page pulled from the free lists via \+\_\+\+\_\+isolate\+\_\+free\+\_\+page back to the free lists they were pulled from.

\+\_\+\+\_\+page\+\_\+set\+\_\+anon\+\_\+rmap -\/ set up new anonymous rmap

@vma\+: VM area to add page to. @address\+: User virtual address of the mapping @exclusive\+: the page is exclusively owned by the current process 