add\+\_\+to\+\_\+page\+\_\+cache\+\_\+locked -\/ add a locked page to the pagecache

@mapping\+: the page\textquotesingle{}s \mbox{\hyperlink{structaddress__space}{address\+\_\+space}} @offset\+: page index @gfp\+\_\+mask\+: page allocation mode

This function is used to add a page to the pagecache. It must be locked. This function does not add the page to the L\+RU. \mbox{\hyperlink{classThe}{The}} caller must do that.

Return\+: \%0 on success, negative error code otherwise.

mem\+\_\+cgroup\+\_\+css\+\_\+from\+\_\+page -\/ css of the memcg associated with a page

If memcg is bound to the default hierarchy, css of the memcg associated with

\+\_\+\+\_\+mem\+\_\+cgroup\+\_\+charge -\/ charge a newly allocated page to a cgroup

@mm\+: mm context of the victim @gfp\+\_\+mask\+: reclaim mode

Try to charge

mem\+\_\+cgroup\+\_\+swapin\+\_\+charge\+\_\+page -\/ charge a newly allocated page for swapin

@mm\+: mm context of the victim @gfp\+: reclaim mode @entry\+: swap entry for which the page is allocated

This function charges a page allocated for swapin. Please call this before adding the page to the swapcache.

Returns 0 on success. Otherwise, an error code is returned.

\+\_\+\+\_\+mem\+\_\+cgroup\+\_\+uncharge -\/ uncharge a page

Uncharge a page previously charged with \mbox{\hyperlink{memcontrol_8c_a2b352dd524cd15d536fbfa8a765cf1af}{\+\_\+\+\_\+mem\+\_\+cgroup\+\_\+charge()}}.

mpol\+\_\+misplaced -\/ check whether current page node is valid in policy

@vma\+: vm area where page mapped @addr\+: virtual address where page mapped

Lookup current policy node id for vma,addr and \char`\"{}compare to\char`\"{} page\textquotesingle{}s node id. Policy determination \char`\"{}mimics\char`\"{} alloc\+\_\+page\+\_\+vma(). Called from fault path where we know the vma and faulting address.

Return\+: N\+U\+M\+A\+\_\+\+N\+O\+\_\+\+N\+O\+DE if the page is in a node that is valid for this policy, or a suitable node ID to allocate a replacement page from.

munlock\+\_\+vma\+\_\+page -\/ munlock a vma page

returns the size of the page as a page mask (0 for normal page, H\+P\+A\+G\+E\+\_\+\+P\+M\+D\+\_\+\+NR -\/ 1 for T\+HP head page)

called from munlock()/munmap() path with page supposedly on the L\+RU. When we munlock a page, because the vma where we found the page is being munlock()ed or munmap()ed, we want to check whether other vmas hold the page locked so that we can leave it on the unevictable lru list and not bother vmscan with it. However, to walk the page\textquotesingle{}s rmap list in \mbox{\hyperlink{rmap_8c_ab6012cc8920afb826bb1e3e5cd80cf94}{page\+\_\+mlock()}} we must isolate the page from the L\+RU. If some other task has removed the page from the L\+RU, we won\textquotesingle{}t be able to do that. So we clear the Page\+Mlocked as we might not get another chance. If we can\textquotesingle{}t isolate the page, we leave it for \mbox{\hyperlink{mm_2internal_8h_af1e9e30255520f222b4b4761bf018ac0}{putback\+\_\+lru\+\_\+page()}} and vmscan \mbox{[}\mbox{\hyperlink{rmap_8c_a81111cdf3621a2f26a915031cf1a3c29}{page\+\_\+referenced()}}/try\+\_\+to\+\_\+unmap()\mbox{]} to deal with.

page\+\_\+remove\+\_\+rmap -\/ take down pte mapping from a page

@compound\+: uncharge the page as compound or small page

\mbox{\hyperlink{classThe}{The}} caller needs to hold the pte lock.

slab\+\_\+destroy -\/ destroy and release all objects in a slab @cachep\+: cache pointer being destroyed

Destroy all the objs in a slab page, and release the mem back to the system. Before calling the slab page must have been unlinked from the cache. \mbox{\hyperlink{classThe}{The}} \mbox{\hyperlink{structkmem__cache__node}{kmem\+\_\+cache\+\_\+node}} -\/$>$list\+\_\+lock is not held/needed.

deactivate\+\_\+file\+\_\+page -\/ forcefully deactivate a file page

This function hints the VM that

mark\+\_\+page\+\_\+lazyfree -\/ make an anon page lazyfree

\mbox{\hyperlink{mm_2swap_8c_a32522ef0c1c8d91f602308a7f86c875d}{mark\+\_\+page\+\_\+lazyfree()}} moves

add\+\_\+to\+\_\+swap -\/ allocate swap space for a page

Allocate swap space for the page and add the page to the swap cache. Caller needs to hold the page lock.

putback\+\_\+lru\+\_\+page -\/ put previously isolated page onto appropriate L\+RU list

Add previously isolated

isolate\+\_\+lru\+\_\+page -\/ tries to isolate a page from its L\+RU list

Isolates a

workingset\+\_\+activation -\/ note a page activation

obj\+\_\+to\+\_\+location -\/ get ($<$page$>$, $<$obj\+\_\+idx$>$) from encoded object value @obj\+: the encoded object value

@obj\+\_\+idx\+: object index

location\+\_\+to\+\_\+obj -\/ get obj value encoded from ($<$page$>$, $<$obj\+\_\+idx$>$)

@obj\+\_\+idx\+: object index 