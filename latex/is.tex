\+\_\+\+\_\+replace\+\_\+page -\/ replace page in vma by new page. based on replace\+\_\+page in mm/ksm.\+c

@vma\+: vma that holds the pte pointing to page @addr\+: address the old @old\+\_\+page\+: the page we are replacing by new\+\_\+page @new\+\_\+page\+: the modified page we replace page by

If @new\+\_\+page is N\+U\+LL, only unmap @old\+\_\+page.

Returns 0 on success, negative error code otherwise.

freed anonymous page (e.\+g. via M\+A\+D\+V\+\_\+\+F\+R\+EE). Returns 0 if

anonymous page, a tmpfs page or otherwise ram or swap backed page. Used by functions that manipulate the L\+RU lists, to sort a page onto the right L\+RU list.

We would like to get this info without a page flag, but the state needs to survive until the page is last deleted from the L\+RU, which could be as far down as \+\_\+\+\_\+page\+\_\+cache\+\_\+release.

until it is released.

If memcg is bound to a traditional hierarchy, the css of root\+\_\+mem\+\_\+cgroup is returned.

and return its inode number or 0 if

is safe to call this function without holding a reference to

for example if its invalidation fails due to the page being dirty or under writeback. 